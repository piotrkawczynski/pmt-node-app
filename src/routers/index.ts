import { router as auth } from "./auth"
// import { router as users } from "./users"
import { router as permissions } from "./permissions"
import { router as projects } from "./projects"
import { router as tags } from "./tags"
import { router as statuses } from "./statuses"
import { router as invites } from "./invites"
import { router as issues } from "./issues"
import { router as sprints } from "./sprints"
import { router as comments } from "./comments"

export {
  auth,
  // users,
  projects,
  tags,
  permissions,
  statuses,
  invites,
  issues,
  comments,
  sprints,
}
